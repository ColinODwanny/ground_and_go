<?xml version="1.0" encoding="utf-8" ?>
<!--Aidan Trusky-->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ground_and_go.Pages.Profile.ProfilePage"
             Title="Profile"
             BackgroundColor="#F0F0F0">

    <ScrollView>
        <VerticalStackLayout Spacing="0" Padding="0" VerticalOptions="Start">
            
            <!-- My Workouts -->
            <Border Stroke="LightGray"
                    StrokeThickness="1"
                    BackgroundColor="White"
                    HeightRequest="80"
                    Margin="0,0,0,1">
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,2" Opacity="0.1" />
                </Border.Shadow>
                
                <Grid ColumnDefinitions="Auto,*" Padding="20">
                    <Label Grid.Column="0"
                           Text="💪"
                           FontSize="32"
                           VerticalOptions="Center"/>
                    
                    <Label Grid.Column="1"
                           Text="My Workouts"
                           FontSize="22"
                           FontAttributes="Bold"
                           TextColor="Black"
                           VerticalOptions="Center"
                           Margin="20,0,0,0"/>
                </Grid>
            </Border>

            <!-- My Journal Entries -->
            <Border Stroke="LightGray"
                    StrokeThickness="1"
                    BackgroundColor="White"
                    HeightRequest="80"
                    Margin="0,0,0,1">
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,2" Opacity="0.1" />
                </Border.Shadow>
                
                <Grid ColumnDefinitions="Auto,*" Padding="20">
                    <Label Grid.Column="0"
                           Text="📖"
                           FontSize="32"
                           VerticalOptions="Center"/>
                    
                    <Label Grid.Column="1"
                           Text="My Journal Entries"
                           FontSize="22"
                           FontAttributes="Bold"
                           TextColor="Black"
                           VerticalOptions="Center"
                           Margin="20,0,0,0"/>
                </Grid>
            </Border>

            <!-- Logout -->
            <Border Stroke="LightGray"
                    StrokeThickness="1"
                    BackgroundColor="White"
                    HeightRequest="80">
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,2" Opacity="0.1" />
                </Border.Shadow>
                
                <Grid ColumnDefinitions="Auto,*" Padding="20">
                    <Label Grid.Column="0"
                           Text="🚪"
                           FontSize="32"
                           VerticalOptions="Center"/>
                    
                    <Label Grid.Column="1"
                           Text="Logout"
                           FontSize="22"
                           FontAttributes="Bold"
                           TextColor="Black"
                           VerticalOptions="Center"
                           Margin="20,0,0,0"/>
                </Grid>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>